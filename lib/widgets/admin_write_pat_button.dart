import 'package:flutter/material.dart';

class AdminWritePatButton extends StatelessWidget {
  const AdminWritePatButton({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
